<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssyt.tqserver.mapper.ChatRoomMapper">

    <select id="selectByAccountIds" resultType="com.ssyt.tqserver.entity.ChatRoom">
        select * from chat_room where account_ids = #{accountIds}
    </select>
</mapper>
