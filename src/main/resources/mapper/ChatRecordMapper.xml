<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssyt.tqserver.mapper.ChatRecordMapper">

    <select id="listByChatRoomId" resultType="com.ssyt.tqserver.entity.ChatRecord">
        select * from chat_record where chat_room_id = #{chatRoomId}
    </select>
</mapper>
